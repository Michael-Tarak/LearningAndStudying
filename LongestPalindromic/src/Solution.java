class Solution {
    public String longestPalindrome(String s) {
        var longestWord = new String();
        var longestLength = 0;
        if (s.length() < 1){
            longestLength = 1;
            for (int i = 1; i < s.length() - 1; i++){
                step = 1;
                var
                while (i - step >= 0 && i + step < s.length()){
                    if (s.indexOf(1 - step) == s.indexOf(1 + step)){
                        if (longestLength < )
                    }
                }
            }
        }

    }
}
